(function() {
  "use strict";

  App.Store = DS.Store.extend({
    adapter: DS.ActiveModelAdapter.extend({
      namespace: '<%= File.join(Blogelator::Engine.routes.url_helpers.root_path, "api").gsub(/^\//, "") %>'
    })
  });

})();
